import 'package:flutter/material.dart';

import '../ui/views/profile_view.dart';

class AppLogic {
  static login(String name, String password, ) {
    /// toda la logica del login
    ///
    /// consumo el api de login
    /// verifico la respueta del api
    /// guardo un jwt en manejador de estado local
    /*
    Navigator.pushNamed(
      context,
      '/profile',
      arguments: ProfileArguments(
        name: "Carlos Sandoval",
        age: 20,
        id: "dfsdfijj939834",
        isAuthenticated: true,
      ),
    );*/
  }
}